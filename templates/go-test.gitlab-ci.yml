variables:
  GO_IMAGE: alpine
  GO_TEST_COMMAND: ""

test:
  stage: build
  image: golang:$GO_IMAGE
  script:
    - apk add --no-cache --no-progress make git
    - make test
  tags:
    - docker
  rules:
    - if: $GO_TEST_COMMAND
