variables:
  GO_IMAGE: alpine

cache:
  paths:
    - vendor/
  key: ${CI_COMMIT_REF_SLUG}

install:
  stage: install
  image: golang:$GO_IMAGE
  script:
    - apk add --no-cache --no-progress make git
    - make install
  artifacts:
    paths:
      - vendor/
  tags:
    - docker
