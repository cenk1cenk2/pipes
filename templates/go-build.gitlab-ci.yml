variables:
  GO_BUILD_COMMAND: task build

build:
  stage: build
  image: cenkcenk1/pipe-go:${GO_VERSION}
  script:
    - $GO_BUILD_COMMAND
  rules:
    - when: on_success
  artifacts:
    paths:
      - ./dist/
  tags:
    - docker
