variables:
  GO_IMAGE: alpine

build:
  stage: build
  image: golang:$GO_IMAGE
  script:
    - apk add --no-cache --no-progress make git
    - make -j4 build
  rules:
    - when: on_success
  artifacts:
    paths:
      - ./dist/
  tags:
    - docker
